# Build and autotest script for MatrixPilot firmware
# http://travis-ci.org

language: c

before_script:
  - pwd
  - ls -la
  - mkdir -p ~/build

script:
  - cd ~/build
  - pwd
  - make -f ../makefile DEVICE=SILSIM

after_success:
  - cd ~
