# Build and autotest script for MatrixPilot firmware
# http://travis-ci.org

language: c

before_script:
  - mkdir -p ~/build

script:
  - cd ~/build
  - make -f ../makefile DEVICE=SILSIM

after_success:
  - cd ~
